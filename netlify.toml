[build]
  publish = "dist"
  functions = "netlify/functions"
  command = "node --check app.js && node --check server.mjs && node --check netlify/functions/api.mjs && node scripts/build-static.mjs"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/build/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
