{
  "name": "boba-and-chaos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node --check app.js && node --check server.mjs && node --check netlify/functions/api.mjs",
    "test": "node --test test/*.test.mjs"
  }
}

